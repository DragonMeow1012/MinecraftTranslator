plugins {
    id 'java'
    id 'net.neoforged.moddev' version '2.0.141'
}

version = project.mod_version
group = project.maven_group
base { archivesName = 'mctranslator' }

// Final jar name: mctranslator-<version>-NeoForge-1.21.1.jar
tasks.named('jar') {
    archiveClassifier = 'NeoForge-1.21.1'
    from('../LICENSE') {
        rename { 'LICENSE_MinecraftTranslator' }
    }
}

repositories {
    mavenCentral()
}

sourceSets {
    main {
        java {
            srcDir '../src/main/java'
            exclude 'com/borwen/mctranslator/fabric/**'
        }
    }
}

neoForge {
    version = project.neoforge_version

    runs {
        client {
            client()
        }
    }

    mods {
        mctranslator {
            sourceSet sourceSets.main
        }
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
    options.release = 21
}

processResources {
    inputs.property 'version', project.version
    from('../src/main/resources/assets/mctranslator/lang') {
        into 'assets/mctranslator/lang'
    }
    filesMatching('META-INF/neoforge.mods.toml') {
        expand 'version': project.version
    }
}
